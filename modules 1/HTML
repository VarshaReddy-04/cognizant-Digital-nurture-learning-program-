<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Local Community Event Portal</title>
  <style>
    #welcomeBanner {
      background-color: lightblue;
      padding: 10px;
      text-align: center;
    }
    .highlight {
      color: green;
      font-weight: bold;
    }
    img.event-img {
      border: 2px solid #444;
      width: 100px;
      height: auto;
    }
    table {
      margin: 20px auto;
      text-align: center;
    }
    form {
      margin: 20px;
    }
  </style>
</head>
<body>

  <!-- Navigation -->
  <nav>
    <a href="#home">Home</a> |
    <a href="#events">Events</a> |
    <a href="#contact">Contact</a> |
    <a href="help.html" target="_blank">Help</a>
  </nav>

  <!-- Welcome Banner -->
  <div id="welcomeBanner">
    <h1>Welcome to Our Community Events Portal</h1>
    <p>Special Offer: <span style="color:red; font-weight:bold;">Free entry</span> for the first 50 registrations!</p>
  </div>

  <!-- Events Section -->
  <section id="events">
    <h2>Gallery of Past Events</h2>
    <table border="1">
      <caption class="highlight">Highlights from Last Year</caption>
      <tr>
        <td><img src="event1.jpg" class="event-img" alt="Event 1" title="Music Night"></td>
        <td><img src="event2.jpg" class="event-img" alt="Event 2" title="Food Fest"></td>
        <td><img src="event3.jpg" class="event-img" alt="Event 3" title="Art Display"></td>
      </tr>
      <tr>
        <td><img src="event4.jpg" class="event-img" alt="Event 4" title="Dance Show"></td>
        <td><img src="event5.jpg" class="event-img" alt="Event 5" title="Kids Carnival"></td>
        <td><img src="event6.jpg" class="event-img" alt="Event 6" title="Cultural Parade"></td>
      </tr>
    </table>
  </section>

  <!-- Registration Form -->
  <section id="contact">
    <h2>Register for an Event</h2>
    <form onsubmit="handleSubmit(); return false;">
      Name: <input type="text" name="name" placeholder="Your Name" required autofocus><br><br>
      Email: <input type="email" name="email" placeholder="you@example.com" required><br><br>
      Event Date: <input type="date" name="date" required><br><br>
      Event Type:
      <select name="eventType" id="eventType" onchange="storePreference()">
        <option value="music">Music</option>
        <option value="food">Food</option>
        <option value="art">Art</option>
      </select><br><br>
      Phone: <input type="text" id="phone" onblur="validatePhone()" placeholder="e.g., 9876543210"><br><br>
      Message:<br>
      <textarea id="message" rows="4" cols="40" onkeyup="countChars()"></textarea><br>
      <small>Characters: <span id="charCount">0</span></small><br><br>
      <button type="submit" onclick="confirmSubmit()">Submit</button>
      <output id="confirmation"></output>
      <br><br>
      <button type="button" onclick="clearPreferences()">Clear Preferences</button>
    </form>
  </section>

  <!-- Promo Video -->
  <section>
    <h2>Promo Video</h2>
    <video width="320" controls oncanplay="videoReady()">
      <source src="promo.mp4" type="video/mp4">
      Your browser does not support the video tag.
    </video>
    <p id="videoMsg"></p>
  </section>

  <!-- Geolocation -->
  <section>
    <h2>Find Nearby Events</h2>
    <button onclick="getLocation()">Find Nearby Events</button>
    <p id="locationResult"></p>
  </section>

  <script>
    function handleSubmit() {
      document.getElementById("confirmation").textContent = "✅ Registered successfully!";
    }

    function validatePhone() {
      const phone = document.getElementById("phone").value;
      if (!/^[6-9]\d{9}$/.test(phone)) {
        alert("Invalid phone number!");
      }
    }

    function countChars() {
      const count = document.getElementById("message").value.length;
      document.getElementById("charCount").textContent = count;
    }

    function videoReady() {
      document.getElementById("videoMsg").textContent = "🎥 Video ready to play!";
    }

    function confirmSubmit() {
      alert("Thanks for submitting!");
    }

    function storePreference() {
      const type = document.getElementById("eventType").value;
      localStorage.setItem("preferredEvent", type);
    }

    function clearPreferences() {
      localStorage.clear();
      sessionStorage.clear();
      alert("Preferences cleared!");
    }

    function getLocation() {
      if (!navigator.geolocation) {
        document.getElementById("locationResult").textContent = "Geolocation not supported!";
        return;
      }
      navigator.geolocation.getCurrentPosition(
        (position) => {
          const coords = `Latitude: ${position.coords.latitude}, Longitude: ${position.coords.longitude}`;
          document.getElementById("locationResult").textContent = coords;
        },
        (error) => {
          document.getElementById("locationResult").textContent = "Location access denied or unavailable.";
        },
        {
          enableHighAccuracy: true,
          timeout: 10000
        }
      );
    }

    // Auto-load saved preference
    window.onload = function () {
      const saved = localStorage.getItem("preferredEvent");
      if (saved) {
        document.getElementById("eventType").value = saved;
      }
    };

    // Warn if leaving unfinished form
    window.onbeforeunload = function () {
      return "You have unsaved changes. Are you sure you want to leave?";
    };
  </script>

</body>
</html>
